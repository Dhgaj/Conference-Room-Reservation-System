# Flask 应用程序密钥，用于加密 session 和 cookies
# 生产环境必须使用强随机密钥，可以使用以下命令生成：
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# 数据库连接字符串
# SQLite: sqlite:///meeting_rooms.db
# MySQL: mysql://user:password@localhost/dbname
# PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URI=sqlite:///meeting_rooms.db

# Session Cookie 安全设置
# True: 仅通过 HTTPS 传输（生产环境必须）
# False: 允许 HTTP 和 HTTPS（开发环境）
SESSION_COOKIE_SECURE=False

# "记住我" Cookie 安全设置
# True: 仅通过 HTTPS 传输（生产环境必须）
# False: 允许 HTTP 和 HTTPS（开发环境）
REMEMBER_COOKIE_SECURE=False

# Flask 运行环境
# development: 开发环境（加载 .env 文件）
# production: 生产环境（加载 .env.production 文件）
FLASK_ENV=development

# Flask 调试模式
# True: 开发模式，显示详细错误信息
# False: 生产模式，隐藏详细错误信息
FLASK_DEBUG=True

# Electron 应用程序 URL
# Electron 桌面应用将加载此 URL
# 示例: https://liansifanfan.pythonanywhere.com
APP_URL=https://your-app-url.com
